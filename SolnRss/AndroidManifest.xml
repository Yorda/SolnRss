<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="free.solnRss"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- ACTIVITY -->
<!--  https://developer.android.com/guide/topics/manifest/activity-element.html#lmode  -->
        <activity
            android:name=".activity.SolnRss"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="free.solnRss.activity.SettingsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="free.solnRss.activity.ReaderActivity"
            android:uiOptions="splitActionBarWhenNarrow" 
            android:hardwareAccelerated="true"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="free.solnRss.activity.SyndicationsCategoriesActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- SERVICE -->
        <service android:name="free.solnRss.service.PublicationsFinderService" />
        
        <!-- service android:name="free.solnRss.service.PublicationsCursorCacheService" / -->
        <!--
        RECEIVER
        <receiver android:name=".receiver.NotificationReceiver" />
        -->

        <provider
            android:name=".provider.PublicationsProvider"
            android:authorities="com.solnRss.provider.publicationprovider"
            android:exported="false" >
        </provider>
        <provider
            android:name=".provider.SyndicationsProvider"
            android:authorities="com.solnRss.provider.syndicationprovider"
            android:exported="false" >
        </provider>
        <provider
            android:name=".provider.CategoryProvider"
            android:authorities="com.solnRss.provider.categoryprovider"
            android:exported="false" >
        </provider>
    </application>

</manifest>